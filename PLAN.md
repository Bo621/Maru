# 마루(Maru) — Track 02 CONSUMER / SOCIAL

> **왜 두 번째 지원인가**: 공고가 「총 5개 트랙에 중복으로 참여할 수 있어요」라고 명시하고,
> 트랙별 최대 3팀을 따로 뽑는다. Track 03(GIWA-NATIVE)은 프로토콜로 냈고,
> Track 02 는 **그 위의 소비자 제품**으로 낸다.
>
> **숨기지 않는다.** 두 지원서를 같은 심사팀이 본다. 「같은 프로토콜, 다른 제품 표면」이라고
> 먼저 밝힌다. 숨기면 들키고, 밝히면 「증명 레이어가 이미 테스트넷에서 돈다」가 강점이 된다.

## 마감

**2026-07-31 (금)** — 이 문서 작성 시점 기준 약 45시간.

## 저장소·폴더 분리 — 먼저 정한다

두 지원서가 **같은 저장소를 가리키면 같은 프로젝트로 보인다.** 분리한다.

```
~/GIWA          POI 프로토콜   github.com/Bo621/POI      (그대로 둔다)
~/Maru          마루(Maru)     github.com/Bo621/Maru      ← 신규
```

| | POI 저장소 | 소셜 저장소 |
|---|---|---|
| 무엇 | 컨트랙트·불변식·검증기·프로토콜 문서 | 소비자 앱 하나 |
| 컨트랙트 | 소유·배포 | **읽기만** — 배포된 주소를 가리킨다 |
| 배포 | `poi-static-production` | **별도 Railway 서비스** |
| 문서 | `vestat.gitbook.io/poi` | README + 자체 문서 |

### 공유 로직을 어떻게 다루나

`@poi/core` 에 순수 로직이 모여 있다 — `deriveState` · `OP`/`RESULT` · `METRICS` · `errors`.
**두 제품이 같은 결정을 다른 상태로 표시하면 치명적이다.** 그래서:

- 소셜 저장소가 `core/` 를 **복사해 담는다**(vendoring). npm 계정·publish 불필요.
- `scripts/sync_core.sh` 가 원본에서 복사하고, **바이트 해시를 비교하는 테스트**를 둔다.
  갈라지면 테스트가 먼저 깨진다 — 사람이 기억할 일이 아니다.

> **왜 복사인가**: npm 미공개 상태에서 git 하위 경로 의존은 안정적이지 않다.
> 서브모듈은 배포 파이프라인이 복잡해진다. 마감 45시간에 쓸 방법이 아니다.

### 지원서에서 밝힐 것

> 「이 제품은 POI 프로토콜(Track 03 지원) 위에 있습니다. 컨트랙트는 공유하고
> 제품과 저장소는 분리돼 있습니다.」

**먼저 밝힌다.** 같은 심사팀이 두 지원서를 본다.

## 무엇을 만드나

트레이더 SNS 의 핵심 난제는 **「이 수익률이 진짜인가」** 다.
지금은 스크린샷과 평판으로 해결하고, 그래서 포토샵과 선택적 자랑이 통한다.

POI 는 그 문제를 이미 푼다 — **결과를 알기 전에 고정하고, 컨트랙트가 산술로 판정한다.**
Track 02 는 그 위에 **사람이 보는 표면**을 올린다.

| 소셜 기능 | 무엇으로 되나 | 새 컨트랙트 |
|---|---|---|
| 피드 — 검증된 판단이 흐른다 | `readDecisionLogs` + 상태 판정 | **불필요** |
| 작성 — 판단을 올린다 | 기존 decision 스키마에 attest | **불필요** |
| 프로필 — 그 사람의 기록 | Passport (`#/passport/<address>`) | **불필요** |
| 팔로우 | 브라우저 로컬 저장 | **불필요** — 온체인이 아니라고 밝힌다 |
| 맞음·틀림 | 업비트 1분봉으로 재계산 | **불필요** |
| 이유 원문 | 공개 파일의 salt·payload 로 커밋 재계산 | **불필요** |
| 입장 변경 이력 | `parents` DAG | **불필요** |
| 조건부 열람 — 「MATCH N건 이상」만 | 온체인 정산 결과로 필터 | **불필요** |
| 인증 배지 — 도장 라벨 | `issuerLabel` 이미 읽는 중 | **불필요** |
| 코멘트 인증 | 새 스키마·리졸버 | **필요 — 이번엔 안 한다** |
| 온체인 팔로우 | 새 스키마 | **필요 — 이번엔 안 한다** |

**표 전체에서 새 컨트랙트가 필요한 칸은 두 개뿐이고, 둘 다 이번 범위 밖이다.**
소셜 표면은 이미 배포된 POI 컨트랙트를 읽고 쓰는 것만으로 선다.

**이번 범위에서 컨트랙트를 건드리지 않는다.** 오늘만 재배포를 세 번 했고
매번 fixture 재생성·문서 전수 갱신·Safe 승인 2회가 따라왔다. 프론트만으로 충분하다.

---

## 화면 1 — 피드 (`#/feed`)

### 보이는 것

여러 지갑의 결정을 **시간 역순**으로 나열한다. 각 줄에:

```
[인장]  0x3f592f21…6938   도장 검증 — TESTNET FAUCET
        BTC_PRICE_KRW_AT_END > 89,291,000
        관측 구간 07-29 16:32 ~ 16:42 UTC        기한초과
```

### 만드는 법

`web/src/read.ts` 의 `readDecisionLogs(schema, attester)` 는 **attester 를 필수로** 받는다.
피드는 attester 없이 전체를 읽어야 하므로 시그니처를 `attester?: Address` 로 바꾼다
(`getLogsChunked` 는 이미 `attester?` 를 옵션으로 받는다 — 확인함).

```ts
// web/src/feed.tsx (신규)
const logs = await readDecisionLogs(SCHEMAS.decision as Hex);   // attester 없이
// 각 uid 에 대해 상태를 구한다 — home.tsx 의 loadSummaries 와 같은 방식
```

**주의 — 이미 겪은 함정**
- `getLogsChunked` 는 90,000 블록씩 끊어 읽는다. `DEPLOY_BLOCK` 부터 `latest` 까지.
- 마지막 청크는 `"latest"` 를 써야 한다. 캐시된 `getBlockNumber` 를 `toBlock` 으로
  쓰면 **방금 채굴된 블록을 놓친다** — 실제로 겪었고 E2E 가 잡았다.
- `readDecision` 은 스키마를 검사한다(`assertSchema`). 다른 스키마 UID 가 섞이면 던진다.
  `Promise.allSettled` 로 감싸고 실패한 건은 **행에서 빼지 말고 오류로 표시**한다.

### 하지 않을 것

- **순위·점수·정렬 자랑을 만들지 않는다.** Passport 에 이미
  「이 목록은 조회된 기록의 나열입니다. 순위나 성과 지표가 아닙니다」가 있다.
  피드에도 같은 문구를 넣는다. **선택 기록으로 순위를 만들면 거짓말이 된다.**
- 건수를 세지 않는다. 「조회된 것이 전부라는 보장은 없습니다」를 표시한다.

---

## 화면 2 — 조건부 열람 (`#/feed?verified=1&match=2`)

### 규칙

「아무나 글을 쓰는 곳」이 아니라 **「증명한 사람의 판단만 보이는 곳」** 을 만든다.

| 조건 | 판정 근거 | 기본값 |
|---|---|---|
| 도장 검증 지갑 | `verifiedAddressUID != 0` 이고 컨트랙트가 통과시킨 것 | 끔 |
| 정산 MATCH N건 이상 | 그 attester 의 결정 중 `SETTLED` 이고 검증기 `MATCH` | 0 |

**MATCH 판정은 온체인만으로 안 된다** — 검증기가 업비트 데이터로 재계산해야 한다.
피드에서 전부 재계산하면 느리다. 그래서 이번 범위는:

- **`SETTLED` 상태 + 활성 정산 존재**까지를 필터 기준으로 쓴다
- 「MATCH 인가」는 결정 상세에서 확인하도록 링크한다
- 이 한계를 화면에 적는다 — **「정산이 등록됐다는 뜻이지, 관측값이 맞다는 뜻이 아닙니다」**

### UI

체크박스 두 개 + 숫자 입력 하나. URL 쿼리로 상태를 유지해 **링크 공유가 되게** 한다.
소셜 제품이므로 「이 조건으로 거른 피드」를 남에게 보낼 수 있어야 한다.

---

## 화면 3 — 프로필 연결

Passport(`#/passport/<address>`)가 이미 있다. 피드의 발행자를 누르면 그리로 간다
(결정 상세에 이미 그 동작이 있다 — `AttesterLink`).

피드 → 프로필 → 결정 상세 → 검증까지 **끊기지 않게** 잇는다. 이게 소셜 제품의 최소 골격이다.

---

## 심사자가 확인할 시나리오

지원서에 그대로 넣는다. **S1~S10 은 지갑 없이 된다.** 작성(S11~S12)만 지갑이 필요하다.

### 읽기 — 지갑 없이

```
S1  /#/feed 를 연다
    → 여러 지갑의 결정이 시간 역순으로 나열된다
    → 「순위가 아닙니다」·「조회된 것이 전부라는 보장은 없습니다」가 보인다

S2  「도장 검증 지갑만」을 켠다
    → 검증 스냅샷이 붙은 결정만 남는다
    → 각 줄에 「도장 검증 — TESTNET FAUCET」 라벨이 보인다

S3  「정산 등록 1건 이상」으로 올린다
    → 아직 결과를 올리지 않은 발행자가 사라진다
    → 기한초과 결정은 남는다 (미발행을 숨기지 않는다)

S4  URL 을 복사해 새 창에 붙인다
    → 같은 필터가 유지된다 (조건부 커뮤니티 링크)

S5  발행자를 누른다 → Passport → 결정 상세 → 「검증하기」
    → 오프체인 재현까지 이어진다

S6  스레드 배지가 붙은 결정을 누른다
    → 같은 지갑이 앞서 올린 판단으로 이어진다 (parents DAG = 입장 변경 이력)

S7  이유 원문이 인용 블록으로 보이고, 해시가 맞는 것에만 일치 표시가 붙는다
    → 파일을 고치면 표시가 사라진다 (원문은 커밋의 증거지, 커밋이 원문을 만들지 않는다)

S8  관측이 끝난 결정에 판정이 붙는다
    → 업비트 1분봉으로 재계산한 맞음·틀림이 함께 나온다
    → 틀린 것을 숨기지 않는다

S9  첫 줄의 팔로우를 누르고 「팔로우」 탭으로 간다
    → 그 지갑의 판단만 남는다
    → 「이 브라우저에만 저장됩니다」 고지가 보인다 (온체인 팔로우가 아니다)

S10 「곧 결과 나옴」 탭을 연다
    → 아직 창이 안 닫힌 판단이 마감 임박 순으로 뜬다
    → 결과를 기다리는 상태가 그대로 보인다
```

### 쓰기 — 지갑 필요 (GIWA Sepolia)

```
S11 지갑 없이 「판단 발행」을 연다
    → 연결 안내가 뜬다 (막다른 길을 만들지 않는다)

S12 지갑을 연결하고 판단을 올린다
    → 관측 구간은 분 경계로 정렬되고, 지난 구간은 거부된다
    → 서명하면 피드 맨 위에 뜨고, 창이 닫히면 판정이 붙는다
```

**S12 가 이 제품의 핵심이다** — 결과를 알기 전에 고정하는 것 말고는 올릴 방법이 없다.

---

## 구현 순서

| | 무엇 | 어디 |
|---|---|---|
| 0 | 저장소 생성 · `core/` vendoring · 동기화 검사 | `~/Maru` |
| 1 | 체인 읽기 — `getLogs` 청크 · `readDecision` · 스키마 가드 | `src/chain.ts` · `src/read.ts` |
| 2 | 피드 화면 + 해시 라우팅 | `src/feed.tsx` · `src/app.tsx` |
| 3 | 필터(도장·정산) + URL 쿼리 동기화 | `src/filter.ts` (**순수 함수**) |
| 4 | 단위 테스트 — 필터 · core 동기화 | `test/` |
| 5 | E2E — 심사 시나리오 전수 | `e2e/feed.spec.ts` |
| 6 | Railway 별도 서비스 배포 | `scripts/` |
| 7 | 디자인 시스템 재정의 · 이유 원문 · 입장 변경 이력 | `src/styles.css` · `src/revealVerify.ts` |
| 8 | 맞음·틀림 판정 — 업비트 재계산 | `src/verdict.ts` · `src/upbit.ts` |
| 9 | 지갑 연결 · 작성 · 팔로우 · 탭 | `src/wallet.ts` · `src/composeDecision.ts` · `src/follow.ts` · `src/feedTab.ts` |

## 검증

```bash
./scripts/run_all_tests.sh          # 게이트 — 실패하면 종료코드 1
./scripts/check_docs_onchain.sh     # 문서 ↔ 온체인
```

**필터는 순수 함수로 뺀다.** 그래야 뮤테이션으로 검증할 수 있다 —
필터를 지웠을 때 테스트가 깨지지 않으면 그 테스트는 무의미하다.

## 하지 말 것

| | 이유 |
|---|---|
| 컨트랙트 수정·재배포 | 오늘만 세 번 했다. fixture·문서·Safe 승인이 전부 따라온다 |
| 순위·점수·리더보드 | POI 가 만들지 않기로 한 것이다. 소셜이라고 예외를 두면 서사가 무너진다 |
| 「MATCH 개수」를 필터로 | 피드에서 전부 재계산하면 느리다. 정산 등록 여부까지만 |
| 좋아요 | 온체인 근거가 없다. 이번 범위 밖 |
| 온체인 팔로우 | 새 스키마가 필요하다. 로컬 팔로우로 대신하고 화면에 밝힌다 |
| 03 제출본 건드리기 | 이미 완성됐다. 02 작업이 03 을 흔들면 안 된다 |
