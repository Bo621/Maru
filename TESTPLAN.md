# 마루 — 배포 전 점검

> **배포는 되돌릴 수 있지만, 심사자가 깨진 화면을 본 것은 되돌릴 수 없다.**
> POI 에서 겪은 실패를 여기서 반복하지 않기 위한 목록이다.

## 게이트 — 하나라도 실패하면 배포하지 않는다

```bash
pnpm test              # 단위 — 필터 순수 함수 · core 동기화
pnpm build             # 타입 + 번들
pnpm test:e2e          # 화면 — S1~S5
./scripts/check_core_sync.sh   # core 가 POI 원본과 바이트 동일한가
```

각 명령은 **실패 시 0 이 아닌 코드로 끝나야 한다.**
POI 에서 `forge test | grep` 파이프가 grep 의 종료코드만 남겨
**테스트가 깨져도 성공으로 끝난** 적이 있다. 파이프로 감싸지 않는다.

## 1. core 동기화 — 가장 먼저

두 제품이 **같은 결정을 다른 상태로 표시하면 치명적이다.**

```bash
./scripts/check_core_sync.sh
```

`~/GIWA/core/src/*.ts` 와 `~/Maru/core/src/*.ts` 의 바이트 해시를 비교한다.
갈라지면 실패한다 — 사람이 기억할 일이 아니다.

> 원본이 없는 환경(CI·심사자 clone)에서는 **건너뛰되 그 사실을 출력**한다.
> 조용히 통과시키면 검사가 아니다.

## 2. 빌드 결과에 실제 주소가 들어갔는가

POI 에서 **Railway 가 업로드된 `dist` 가 아니라 자체 변수로 다시 빌드**해
구 주소가 배포된 적이 있다. 번들 내용을 직접 확인한다.

```bash
grep -c "0x0f25917176a405bb9022e5b417e0d57348b30f89" dist/assets/*.js   # 결정 리졸버
grep -c "0x88990bf8da2b83b2f68c5783dc1a4375f9f956185c6bafcbd97f7de6d5aa3749" dist/assets/*.js
grep -c "127.0.0.1" dist/assets/*.js                                    # 0 이어야 한다
```

**0 이면 빌드 스크립트가 실패로 끝나야 한다.**

## 3. 화면 시나리오 — E2E 로 고정

심사자가 밟을 경로 그대로. **지갑 없이 전부 된다.**

| | 무엇 | 기대 |
|---|---|---|
| S1 | `/#/` 를 연다 | 결정이 시간 역순으로 나열된다 |
| S1b | 안내 문구 | 「순위가 아닙니다」·「조회된 것이 전부라는 보장은 없습니다」 |
| S2 | 「도장 검증만」 켠다 | 검증 스냅샷이 붙은 것만 남고 라벨이 보인다 |
| S3 | 「정산 등록」 켠다 | 정산이 없는 발행자가 사라진다. **기한초과는 남는다** |
| S4 | URL 을 복사해 새 창 | 같은 필터가 유지된다 |
| S5 | 발행자 → 사람 화면 → 판단 열기 | 끊기지 않고 이어진다 |

**필터가 아무것도 거르지 않아도 통과하는 테스트를 쓰지 않는다.**
켰을 때와 껐을 때 **행 수가 달라지는 것**까지 확인한다.

## 4. 뮤테이션 — 테스트가 진짜인지 확인

```bash
# filter.ts 의 조건을 하나씩 지우고 pnpm test 를 돌린다
# 지웠는데 통과하면 그 테스트는 무의미하다
```

POI 에서 새 방어를 넣을 때마다 이 방법으로 확인했고, **실제로 무의미한 테스트를 잡아냈다.**

## 5. 실제 체인 상대 — 배포 직전 1회

로컬 anvil 이 아니라 **GIWA Sepolia** 를 상대로 한 번 띄워 본다.

```bash
pnpm dev    # .env 의 주소로 실제 체인을 읽는다
```

확인할 것:
- 피드에 **최소 5건** 이 보이는가 (POI fixture 5종이 살아 있다)
- 기한초과·등록완료·철회이력이 각각 다른 인장으로 표시되는가
- 도장 검증 결정에 「TESTNET FAUCET」 라벨이 보이는가
- 공개 RPC 가 느릴 때 **빈 화면이 아니라 로딩 상태**가 보이는가

> POI 에서 공개 RPC 가 90,000 블록 제한을 걸어 `fromBlock: 0` 이 실패했다.
> `DEPLOY_BLOCK` 부터 청크로 읽어야 한다. 마지막 청크는 `toBlock: "latest"` —
> 캐시된 블록 번호를 쓰면 **방금 채굴된 것을 놓친다.**

## 6. 배포 후 확인 — 여기까지가 배포다

```bash
curl -sI <URL> | head -1                       # 200
curl -s <URL>/assets/index-*.js | grep -c 0x0f25917176   # 새 주소가 들어갔는가
```

**「배포 시작됨」 메시지를 성공으로 읽지 않는다.**
POI 에서 존재하지 않는 서비스 이름으로 올려 **한 시간을 날렸다** — railway 는
업로드까지 성공시키고 조용히 아무것도 배포하지 않는다.

## 실패 기록

여기에 적는다. 같은 실패를 두 번 하지 않기 위해서다.

| 날짜 | 무엇이 깨졌나 | 어떻게 막았나 |
|---|---|---|
| | | |
