# 마루 — 화면 계획

> POI 는 **증서**다. 마루는 **마루**다 — 사람들이 모여 앉아 이야기하는 곳.
> 같은 데이터를 쓰지만 **다른 물건이어야 한다.** 심사팀이 둘 다 본다.

## 무엇이 달라야 하나

| | POI | 마루 |
|---|---|---|
| 보는 사람 | 검증하는 사람 (심사자·제3자) | **읽는 사람** (다른 트레이더) |
| 단위 | 결정 하나를 깊게 | **흐름** — 여러 사람의 판단이 지나간다 |
| 톤 | 증서·법적 문서. 어둡고 각지다 | 대청마루 — **밝고 넓고, 앉을 자리가 있다** |
| 첫 화면 | 「이 UID 를 조회하세요」 | 「지금 누가 무엇을 걸었나」 |

**POI 의 다크·주황(#FF2200)을 그대로 쓰지 않는다.** 같은 팀이 만든 티는 나되
같은 제품으로 보이면 안 된다.

## 색

한지와 나무. **밝은 바닥에 검은 글씨** — POI 의 정확한 반대다.

```
--floor    oklch(97.5% 0.008 85)    한지 바닥
--grain    oklch(93%   0.012 80)    나뭇결 — 구분선·카드 경계
--ink      oklch(22%   0.015 60)    먹
--ink-soft oklch(48%   0.012 60)    부연 설명
--seal     oklch(52%   0.18  28)    인장 — 붉은 흙빛. GIWA 주황보다 어둡고 차분하다
--jade     oklch(58%   0.10  165)   검증됨 — 도장 배지에만 쓴다
```

> 순수 흰색·검정을 쓰지 않는다. 자연에 없다.
> 다크 모드는 **이번 범위 밖** — 하나를 제대로 하는 편이 낫다.

## 타이포

```
제목   Gowun Batang    — 곧은 바탕. 한글 세리프, 마루의 결
본문   Pretendard      — 이미 프로젝트에 있다
수치   ui-monospace    — UID·가격·시각은 자릿수가 맞아야 읽힌다
```

POI 는 제목에 서양 세리프를 썼다. 마루는 **한글 세리프**를 쓴다 — 한옥의 물건이다.

## 화면 셋

### 1. 피드 `#/`

마루의 첫 화면은 **목록이 아니라 흐름**이다.

```
┌─────────────────────────────────────────────────┐
│  마루                          [도장 검증만] [정산 1건+] │
│  ─────────────────────────────────────────────  │
│                                                  │
│   ⬤ 기한초과   0xA1Cb…F310  · 도장 검증 TESTNET FAUCET │
│     BTC 가 89,291,000 을 넘는다                     │
│     07-29 16:32 → 16:42 UTC        [열기 →]       │
│  ─────────────────────────────────────────────  │
│   ◐ 등록완료   0xA1Cb…F310  · 도장 검증            │
│     ...                                          │
│                                                  │
│  이 목록은 순위가 아닙니다.                          │
│  조회된 것이 전부라는 보장은 없습니다.                 │
└─────────────────────────────────────────────────┘
```

**카드로 감싸지 않는다.** 줄과 여백으로 나눈다 — 마루 널빤지처럼.
POI 가 카드·테두리를 쓰므로 여기서는 쓰지 않는다.

인장은 **왼쪽 원 하나**. 색으로만 상태를 말하지 않는다(색맹 대응) — 글자를 함께 둔다.

### 2. 필터 — 조건부 열람

상단에 토글 둘. 켜면 **URL 이 바뀐다** (`#/?verified=1&settled=1`).

> 소셜 제품이므로 **「이 조건으로 거른 마루」를 링크로 보낼 수 있어야 한다.**
> 그게 「조건이 되는 사람만 들어오는 방」의 최소 형태다.

필터 아래에 반드시 적는다:

```
정산이 등록됐다는 뜻이지, 관측값이 맞다는 뜻이 아닙니다.
맞았는지는 각 판단을 열어 직접 검증하세요.
```

**이 문장이 이 제품의 태도다.** 걸러 준다고 보증하지 않는다.

### 3. 사람 `#/p/<주소>`

한 사람의 판단이 시간순으로. 상단에 도장 배지.

**순위·승률·수익률을 만들지 않는다.** 선택 기록으로 순위를 만들면 거짓말이 된다 —
POI 가 만들지 않기로 한 것이고, 소셜이라고 예외를 두면 서사가 무너진다.

대신 이렇게 적는다:

```
이 사람이 남긴 판단입니다. 승률이 아닙니다.
미발행은 「기한초과」로 남아 있고, 지워지지 않습니다.
```

## 움직임

거의 없다. 다만 **한 곳**에만 둔다 — 피드가 처음 뜰 때 줄이 위에서 순차로 내려온다
(60ms 간격, `ease-out-quart`, 12px). 마루에 사람이 하나씩 앉는 느낌.

`prefers-reduced-motion` 이면 끈다.

## 반응형

| | |
|---|---|
| 모바일 | 한 줄에 하나. 필터는 상단 고정 |
| 데스크톱 | 본문 최대 `68ch` — 읽기 위한 폭이지 채우기 위한 폭이 아니다 |

**모바일에서 기능을 빼지 않는다.** 필터도 링크 공유도 그대로 된다.

## 하지 않을 것

| | 이유 |
|---|---|
| 좋아요·팔로우·댓글 | 온체인 근거가 없다. 마루는 **증명된 것만** 보여준다 |
| 순위·리더보드·승률 | 선택 기록으로 순위를 만들면 거짓말이 된다 |
| 무한 스크롤 | 「조회된 것이 전부가 아니다」라고 말하면서 끝없이 보여주면 모순이다 |
| 다크 모드 | 범위 밖. 하나를 제대로 |
| POI 색·폰트 그대로 | 같은 제품으로 보인다 |
