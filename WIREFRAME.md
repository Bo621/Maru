# 마루 — 화면별 와이어프레임과 기능

> **구현 전에 읽는다.** 화면이 몇 개고, 각 화면이 어떤 상태를 가지며,
> 무엇을 누르면 어디로 가는지가 여기 다 있다.
> 여기 없는 기능은 만들지 않는다 — 만들고 싶으면 이 문서를 먼저 고친다.

## 화면 목록

| 경로 | 이름 | 지갑 필요 |
|---|---|---|
| `#/` | 마루 (피드) | **아니오** |
| `#/p/<주소>` | 사람 | **아니오** |
| 그 외 | 없는 화면 | — |

**세 개뿐이다.** 판단 상세는 만들지 않는다 — POI 가 이미 가지고 있고,
같은 화면을 두 번 만들면 두 제품이 같아 보인다. 「열기」는 POI 로 보낸다.

---

## 1. 마루 `#/`

### 전체 구조

```
┌──────────────────────────────────────────────────────┐
│  마루                                                 │  ← 머리
│  결과를 알기 전에 걸어 둔 판단들                        │
├──────────────────────────────────────────────────────┤
│  [ ] 도장 검증만    [ ] 결과 등록된 것만                │  ← 거르개
│  정산이 등록됐다는 뜻이지, 관측값이 맞다는 뜻이 아닙니다.  │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ⬤ 기한초과                                           │  ← 널
│  0xA1Cb…F310  · 도장 검증 TESTNET FAUCET               │
│  BTC 가 89,291,000 을 넘는다                            │
│  07-29 16:32 → 16:42 UTC                    열기 →     │
│  ──────────────────────────────────────────────────  │
│  ◐ 등록완료                                           │
│  ...                                                  │
│                                                       │
├──────────────────────────────────────────────────────┤
│  이 목록은 순위가 아닙니다.                              │  ← 꼬리
│  조회된 것이 전부라는 보장은 없습니다.                     │
└──────────────────────────────────────────────────────┘
```

### 널 하나 (한 줄)

```
⬤  상태말        ← 인장. 색 + 글자 둘 다. 색만으로 말하지 않는다
   0xA1Cb…F310   ← 발행자. 누르면 사람 화면
   · 도장 검증 TESTNET FAUCET   ← 있을 때만. 없으면 이 조각을 아예 안 그린다
   BTC 가 89,291,000 을 넘는다   ← 사람이 읽는 문장 (metricId·op·threshold 를 옮긴 것)
   07-29 16:32 → 16:42 UTC      열기 →
```

**「미검증 지갑」이라고 쓰지 않는다.** 검증이 없으면 그 줄을 비운다 —
피드는 읽는 곳이지 판정하는 곳이 아니다. 없는 것을 굳이 말하면 낙인이 된다.

### 상태 넷 — 전부 만든다

| 상태 | 화면 |
|---|---|
| 불러오는 중 | 널 3개 자리에 회색 뼈대. **빈 화면을 보여주지 않는다** |
| 하나도 없음 | 「아직 걸린 판단이 없습니다」 + 거르개를 끄라는 안내 |
| 오류 | 「불러오지 못했습니다」 + **다시 시도** 버튼 + 원인 한 줄 |
| 일부 실패 | 읽은 것은 보여주고, 실패한 UID 는 **회색 줄로 남긴다** |

> 마지막이 중요하다. 조용히 빼면 「조회된 것이 전부가 아니다」라는 말과 어긋난다.
> POI 에서 실패한 행의 오류 문구가 **폭 0px 에 갇혀 안 보인** 적이 있다. 폭을 확보한다.

### 상태말과 색

| 상태 | 글자 | 색 |
|---|---|---|
| 대기 | 대기 | `--ink-soft` |
| 관측중 | 관측중 | `--ink` |
| 등록완료 | 등록완료 | `--jade` |
| 기한초과 | 기한초과 | `--seal` |
| 철회이력 | 등록완료 · 정정됨 | `--jade` + 부연 |

`aria-label` 에 상태말을 넣는다. POI 에서 `::before` 가 접근성 이름에 섞여
E2E 11 개가 깨진 적이 있다 — 장식 문자는 `content: "…" / ""` 로 대체 텍스트를 비운다.

### 정렬

**시각 역순 고정.** 정렬 선택지를 주지 않는다 — 순위를 만들지 않기 위해서다.

---

## 2. 거르개 (조건부 열람)

### 두 개뿐

```
[ ] 도장 검증만        verifiedAddressUID != 0 인 판단만
[ ] 결과 등록된 것만    활성 정산이 있는 판단만
```

### URL 과 묶는다

```
#/                      전체
#/?verified=1           도장 검증만
#/?settled=1            결과 등록된 것만
#/?verified=1&settled=1 둘 다
```

**켜면 URL 이 바뀌고, URL 을 열면 켜져 있다.** 뒤로 가기도 동작한다.

> 이게 「조건이 되는 사람만 들어오는 방」의 최소 형태다.
> 링크로 보낼 수 없으면 커뮤니티가 아니다.

### 반드시 함께 보이는 문장

```
정산이 등록됐다는 뜻이지, 관측값이 맞다는 뜻이 아닙니다.
맞았는지는 각 판단을 열어 직접 검증하세요.
```

**걸러 준다고 보증하지 않는다.** 이 문장이 빠지면 제품이 거짓말을 한다.

### 걸러서 0건이 되면

숨기지 않고 말한다 — 「이 조건에 맞는 판단이 없습니다」 + **거르개 끄기** 버튼.

---

## 3. 사람 `#/p/<주소>`

```
┌──────────────────────────────────────────────────────┐
│  ← 마루                                               │
│  0xA1Cb5CbC9D7a0B7164a1bFE4B19bfe1Bf38BF310          │
│  도장 검증 — TESTNET FAUCET                            │  ← 있을 때만
├──────────────────────────────────────────────────────┤
│  (마루와 같은 널 목록, 이 사람 것만)                     │
├──────────────────────────────────────────────────────┤
│  이 사람이 남긴 판단입니다. 승률이 아닙니다.              │
│  미발행은 「기한초과」로 남아 있고, 지워지지 않습니다.       │
└──────────────────────────────────────────────────────┘
```

### 하지 않는 것

- **승률·수익률·건수 집계를 만들지 않는다.** 선택 기록으로 순위를 만들면 거짓말이 된다.
- 팔로우·좋아요 없음. 온체인 근거가 없다.

### 주소 처리

- URL 의 주소는 **소문자로 정규화**한다. POI 에서 대소문자 비교가 세 번 문제였다.
- 형식이 틀리면 「주소 형식이 아닙니다」 + 마루로 돌아가는 링크.

---

## 4. 없는 화면

```
없는 화면입니다.
#/잘못된경로
← 마루
```

POI 에서 `#/mine` 을 추측해 들어갔다가 이 화면을 만난 적이 있다. **경로를 추측하게 두지 않는다** —
머리의 「마루」 글자가 항상 `#/` 로 간다.

---

## 공통

### 머리

```
마루                                    ← 누르면 #/
결과를 알기 전에 걸어 둔 판단들            ← 부제. 제품이 무엇인지 한 줄로
GIWA Sepolia · 91342                    ← 어느 체인인지. 테스트넷임을 숨기지 않는다
```

### 꼬리

```
이 목록은 순위가 아닙니다.
조회된 것이 전부라는 보장은 없습니다.
POI 프로토콜 위에서 동작합니다 → (POI 링크)
```

**마지막 줄이 중요하다.** 심사팀이 두 지원서를 본다. 먼저 밝힌다.

### 반응형

| | 모바일 | 데스크톱 |
|---|---|---|
| 널 | 세로로 쌓임 | 상태·발행자·문장이 한 줄 |
| 거르개 | 상단 고정 | 본문 위 |
| 본문 폭 | 전체 | 최대 `68ch` |

**모바일에서 기능을 빼지 않는다.** 거르개도 링크 공유도 그대로.

### 접근성

- 인장은 색 + 글자. 색맹으로 상태를 잃지 않는다
- 거르개는 `<label>` 로 묶어 글자를 눌러도 켜진다
- 포커스 링을 지우지 않는다
- `prefers-reduced-motion` 이면 등장 움직임을 끈다

---

## 구현 순서 — 이 순서대로 커밋한다

| | 무엇 | 끝났다고 말할 수 있는 조건 |
|---|---|---|
| 1 | 널 하나 그리기 (`Plank`) | 상태 5종이 각각 다른 인장으로 보인다 |
| 2 | 마루 목록 + 상태 4종 | 로딩·빈·오류·일부실패가 각각 보인다 |
| 3 | 거르개 + URL 동기화 | 새 창에 URL 을 붙이면 같은 화면 |
| 4 | 사람 화면 | 마루에서 발행자를 눌러 도달한다 |
| 5 | 없는 화면 · 머리 · 꼬리 | 경로를 틀려도 돌아올 수 있다 |
| 6 | E2E S1~S5 | `TESTPLAN.md` 의 시나리오가 전부 통과 |

**각 단계마다 커밋한다.** 한 번에 다 만들고 한 번에 커밋하면 어디서 깨졌는지 못 찾는다.
